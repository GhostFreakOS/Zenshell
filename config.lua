-- Zen Shell Configuration

-- Active plugins
plugins = {
    "autosuggest.lua"
}

-- Theme settings
theme = {
    prompt_color = "#3498db",  -- Blue color
    prompt_format = "[%u@%h %d]$ "
}

-- Shell settings
settings = {
    history_size = 1000,
    enable_autosuggestions = true
} 